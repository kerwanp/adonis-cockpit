{{{ exports({ to: app.makePath('tailwind.config.ts') }) }}}
import { Config } from 'tailwindcss'
import cockpit from 'adonis-cockpit/tailwind'

export default {
  darkMode: 'class',
  content: ['./resources/views/**/*.edge', cockpit.contentPath],
  plugins: [cockpit],
} as Config
